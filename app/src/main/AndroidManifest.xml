<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Add telephony feature for SMS/phone permissions -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.polaris_client"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAUb7PkYrFw5cs8gDY4nkSMZr03T0H4B2Y"/>

        <activity
            android:name=".views.auth.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.polaris_client.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".views.auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.polaris_client.NoActionBar" />

        <activity
            android:name=".views.auth.SignupActivity"
            android:exported="false"
            android:theme="@style/Theme.polaris_client.NoActionBar" />

        <activity
            android:name=".views.MainActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <activity
            android:name=".views.MapActivity"
            android:exported="true"
            android:label="@string/app_name">
        </activity>
        
        <!-- Background Service for cellular data collection -->
        <service
            android:name=".controllers.BackgroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />
        
        <!-- Register broadcast receiver for scheduled SMS tests -->
        <receiver 
            android:name=".views.ui.tests.SmsTestFragment$SmsAlarmReceiver" 
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.polaris_client.SMS_TEST_ALARM" />
            </intent-filter>
        </receiver>

    </application>
</manifest>